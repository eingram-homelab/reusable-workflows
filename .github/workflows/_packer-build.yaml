---
name: Packer Build

on:
  workflow_call:
    inputs:
      repository:
        description: 'Repository containing Packer templates'
        type: string
        default: ''
      ref:
        description: 'Branch or ref to check out'
        type: string
        default: ''
      dir_names:
        description: 'Space-separated list of packer directories to build. Overrides changed-files detection when provided (e.g. for manual dispatch).'
        type: string
        default: ''
    secrets:
      VAULT_TOKEN:
        description: 'Vault token with permissions to access secrets needed for build'
        required: true
      # GCP_STORAGE:
      #   description: 'Base64-encoded GCP service account key for Terraform template verification'
      #   required: true
    outputs:
      TMPL_NAME:
        description: 'Timestamped vSphere template name produced by packer build'
        value: ${{ jobs.build.outputs.TMPL_NAME }}

jobs:
  build:
    outputs:
      TMPL_NAME: ${{ steps.set_TMPL_NAME.outputs.TMPL_NAME }}
    name: Packer Build
    runs-on: arc-runners
    env:
      VAULT_ADDR: ${{ vars.VAULT_ADDR }}
      VAULT_TOKEN: ${{ secrets.VAULT_TOKEN }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          repository: ${{ inputs.repository || github.repository }}
          ref: ${{ inputs.ref || github.head_ref }}

      - name: Setup Packer
        uses: hashicorp/setup-packer@v3.2.0

      - name: Run packer init
        env:
          DIR_NAMES: ${{ inputs.dir_names }}
        run: |
          for dir in $DIR_NAMES; do
            echo "::notice::Running packer init for $dir"
            packer init "$dir"
          done

      - name: Run packer build
        env:
          DIR_NAMES: ${{ inputs.dir_names }}
        run: |
          # xorriso needed for vsphere-iso to build Windows template
          sudo apt-add-repository universe
          sudo apt-get install -y xorriso
          for dir in $DIR_NAMES; do
            echo "::group::Building $dir"
            if ! packer build "$dir"; then
              echo "::error::Packer build failed for $dir"
              echo "::endgroup::"
              exit 1
            fi
            echo "::endgroup::"
          done

      - name: Set vars from artifact info
        env:
          DIR_NAMES: ${{ inputs.dir_names }}
        id: set_TMPL_NAME
        run: |
          for dir in $DIR_NAMES; do
            echo "::notice::Setting vars for $dir"
            echo "TMPL_NAME=$(jq -r '.builds[0].artifact_id' "$dir/build-manifest.json")" >> "$GITHUB_OUTPUT"
          done
